{
  "name": "products",
  "version": "1.0.0",
  "description": "Service OData V4 - Products",
  "repository": "https://github.com/Logali-Group/C24C316.git",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^8.4.2",
    "express": "^4.21.1"
  },
  "devDependencies": {
    "@cap-js/cds-types": "^0.7.0",
    "@cap-js/sqlite": "^1.7.7"
  },
  "cds": {
    "requires": {
      "[dev]": {
        "db": {
          "kind": "sqlite",
          "credentials": {
            "database": "db.sqlite"
          }
        },
        "auth": "dummy"
      }
    }
  },
  "scripts": {
    "start": "cds watch --profile dev --port 4005",
    "start-dev": "cds-serve --profile dev"
  }
}
